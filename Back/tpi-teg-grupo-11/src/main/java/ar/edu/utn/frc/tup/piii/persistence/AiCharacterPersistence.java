package ar.edu.utn.frc.tup.piii.persistence;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

@Service
@RequiredArgsConstructor
public class AiCharacterPersistence {
//
//    private final AiCharacterRepository repository;
//    private final ModelMapper modelMapper;
//
//    public List<AICharacter> findAllCharacters() {
//        return repository.findAll().stream()
//                .map(entity -> modelMapper.map(entity, AICharacter.class))
//                .collect(Collectors.toList());
//    }
//
//    public AICharacter findCharacterById(Long id) {
//        return modelMapper.map(repository.getReferenceById(id), AICharacter.class);
//    }
//
//    public List<AICharacter> findAllCharactersByAiProfile(AIProfile profile){
//        return repository.getAllByProfile(profile).stream()
//                .map(entity ->modelMapper.map(entity, AICharacter.class))
//                .collect(Collectors.toList());
//    }
}
